# TODO

* Add metrics (SNMP+AgentX, prometheus-cpp)
* Add admin API (nplexadm via uv_pipe_t, stats, conf, purge, rollback, rotate)
* Support multiple journals (rotate-journal-max-bytes, rotate-journal-max-entries, rotate-journal-daily-at, max-open-archives)
* Create timer to check if msg delivery exceeds X millis
* Considere to compress snapshots (see https://github.com/mateidavid/zstr)
* Rename nplexcat to nplex_dump (binary -> json)
* Considere to create nplex_restore (json -> binary)
